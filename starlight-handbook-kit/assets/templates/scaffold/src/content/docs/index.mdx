---
title: {{SITE_TITLE}}
description: {{SITE_DESCRIPTION}}
template: splash
hero:
  tagline: {{SITE_DESCRIPTION}}
  actions:
    - text: Start with the Example Cluster
      link: {{DEPLOY_BASE}}/example-cluster/
      icon: right-arrow
      variant: primary
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

export const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + '/';

Every topic follows the same shape — **Overview → Mental model → Types → When to use / When NOT → Tradeoffs → Diagram → Try it → Real-world examples → Further reading** — so you can scan for the decision, not just the definition.

## Clusters

<CardGrid>
  <LinkCard title="Example Cluster" href={`${base}example-cluster/`} description="A worked example cluster. Duplicate it to start your own." />
</CardGrid>
