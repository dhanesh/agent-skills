# Tmux Agent Herdr-Lite integration
# Source from ~/.tmux.conf:
#   source-file ~/.tmux/agent-panes/tmux-agent.conf

# Human-friendly defaults inspired by Zellij: mouse, vi copy mode, discoverable menus,
# easy pane navigation/resizing, zoom, layouts, session tree, and popup dashboard.
set -g mouse on
set -g status-interval 5
set -g mode-keys vi
set -g pane-border-status top
set -g pane-border-format ' #P #{pane_current_command} #{pane_title} '
setw -g automatic-rename on
setw -g aggressive-resize on
# Focus events let the scanner treat a focused pane as "viewed" (done -> idle,
# Herdr-style) and keep pane titles fresh.
set -g focus-events on

# Keep existing left side simple; append agent summary to right side.
# If you already have a custom status-right, merge this manually rather than overwriting.
set -g status-left '#[bold]#S #[default]'
set -g status-right '#(@AGENT_BIN@/agent-status-summary) #[default]%H:%M'

# Discoverability: Zellij-like help/dashboard and command menu.
bind-key ? display-popup -E -w 90% -h 80% '@AGENT_BIN@/agent-dashboard; printf "\nPress Enter..."; read'
bind-key m run-shell '@AGENT_BIN@/agent-menu'
bind-key S choose-tree -Zw

# Jump bindings end with '|| true' so a no-match (agent-jump already shows a
# status-line message) doesn't trigger tmux's noisy "returned 1" run-shell dump.
# Prefix + g: jump to first blocked agent
bind-key g run-shell '@AGENT_BIN@/agent-jump blocked || true'

# Prefix + E: jump to first crashed/errored agent
bind-key E run-shell '@AGENT_BIN@/agent-jump error || true'

# Prefix + G: refresh status scan
bind-key G run-shell '@AGENT_BIN@/agent-status-scan'

# Prefix + W/i/D: jump by state (idle also matches done — finished, unviewed).
# Lowercase i on purpose: prefix+I belongs to TPM's plugin installer.
bind-key W run-shell '@AGENT_BIN@/agent-jump working || true'
bind-key i run-shell '@AGENT_BIN@/agent-jump idle || true'
bind-key D run-shell '@AGENT_BIN@/agent-jump done || true'

# Zellij-like pane ergonomics.
bind-key | split-window -h
bind-key - split-window -v
bind-key Tab last-pane
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key H resize-pane -L 5
bind-key J resize-pane -D 3
bind-key K resize-pane -U 3
bind-key L resize-pane -R 5
bind-key z resize-pane -Z
bind-key Space next-layout
bind-key r source-file ~/.tmux.conf \; display-message 'tmux config reloaded'

# Better copy/search defaults. In copy mode: / search, v begin selection, y copy.
bind-key [ copy-mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'pbcopy 2>/dev/null || xclip -selection clipboard 2>/dev/null || wl-copy 2>/dev/null || cat >/dev/null'
