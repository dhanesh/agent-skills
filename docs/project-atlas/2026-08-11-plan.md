# Task plan — project-atlas — index every project on a machine into a durable, searchable, cloud-replicated SQLite database

Derived from `2026-08-11-spec.md`. One task per requirement; each task names the check that proves it done.

## T1 — `atlas scan` must identify a directory as a project root when it contains a `.git`

- Satisfies: R1
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Build a fixture tree with a git repo, a `pyproject.toml` project, and a

## T2 — A second `atlas scan` over unchanged roots must leave the number of rows in the

- Satisfies: R2
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Run `atlas scan` twice on the same fixture; `SELECT count(*) FROM projects` returns

## T3 — For a project root that is a git working tree, `atlas scan` must record its origin

- Satisfies: R3
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] On a fixture git repo with an origin remote and one commit, the project's row has

## T4 — When a previously indexed project path no longer exists, `atlas scan` must retain its

- Satisfies: R4
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Scan a fixture, delete one project directory, scan again; that project's row still

## T5 — Every connection `atlas` opens against the index must set `journal_mode=WAL` and must

- Satisfies: R5
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] After any `atlas` command touches the index, `PRAGMA journal_mode` on the file

## T6 — `atlas search` must use an FTS5 index when the SQLite build provides FTS5, and must

- Satisfies: R6
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `atlas search stripe` returns the seeded project on a build with FTS5; with FTS5

## T7 — The searchable document for a project must include its path, name, and the text of

- Satisfies: R7
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Seed a project whose README is 200000 bytes; the stored document for that project is

## T8 — `atlas litestream-config` must emit the Litestream 0.5.x singular `replica:` mapping

- Satisfies: R8
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `atlas litestream-config --target r2 ...` output parsed as YAML has

## T9 — When the target is Cloudflare R2, the generated replica URL must carry an `endpoint`

- Satisfies: R9
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] The same output's `dbs[0].replica.url` contains `endpoint=https://` and the

## T10 — The generated configuration must set an explicit `sync-interval` rather than

- Satisfies: R10
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] The same output contains a `sync-interval:` key with a non-empty value.

## T11 — The generated configuration must reference credentials as environment-variable

- Satisfies: R11
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Running the generator with `AWS_SECRET_ACCESS_KEY` set in the environment produces

## T12 — `atlas doctor` must report FTS5 availability, the index journal mode, and whether a

- Satisfies: R12
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `atlas doctor --json` emits an object with `fts5`, `journal_mode`, and `litestream`

## T13 — `atlas litestream-config` must also print the exact `litestream restore` command for

- Satisfies: R13
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `atlas litestream-config` output includes a line beginning `litestream restore`

## T14 — The shipped tooling must run on python3 with the standard library only, and must

- Satisfies: R14
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `grep -rE '^(import|from) ' project-atlas/assets/*.py` lists only standard-library

## T15 — The skill must ship a stdlib unit suite and an `eval/run_eval.py` outcome eval whose

- Satisfies: R15
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `python3 project-atlas/eval/run_eval.py` prints `EVAL_RESULT: PASS (n/n checks)`,

## T16 — `atlas sessions ingest` must record, for each discovered agent session, its session

- Satisfies: R16
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] After ingesting a fixture agent home, the session's row has the fixture's cwd,

## T17 — Every record in a captured transcript must be stored verbatim as its own row in

- Satisfies: R17
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] A fixture transcript of 3 records — one of them a type the tool has no special

## T18 — Reconstructing an unredacted capture must produce a byte-for-byte copy of the

- Satisfies: R18
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Ingest a fixture with `--no-redact`, restore it, and `cmp` the restored file against

## T19 — `atlas sessions ingest` must replace a previously captured session's events rather

- Satisfies: R19
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Ingest a 2-record transcript, truncate it to 1 record, ingest again; the session has

## T20 — Session capture must redact credential-shaped strings from transcripts and side

- Satisfies: R20
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Ingest a transcript containing a planted `DB_PASSWORD=` value; that value appears

## T21 — Session capture must never read the agent's account configuration file, and must

- Satisfies: R21
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] With an account-config file and a shell snapshot present in the fixture, a default

## T22 — `atlas sessions restore` must write the transcript to the target machine's project

- Satisfies: R22
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Restore with `--cwd /Users/dev/payments` writes

## T23 — `atlas sessions restore` must refuse to run when the stored parent chain has a

- Satisfies: R23
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] Each of the three refusals exits 1 with a message naming the cause: deleting the

## T24 — Captured chats must be full-text searchable through `atlas sessions search`, using

- Satisfies: R24
- Where: unspecified — fill in during plan review
- Verify:
  - [ ] `atlas sessions search webhook --json` returns the fixture session id, and the same

## Coverage

| Requirement | Task(s) |
|---|---|
| R1 | T1 |
| R2 | T2 |
| R3 | T3 |
| R4 | T4 |
| R5 | T5 |
| R6 | T6 |
| R7 | T7 |
| R8 | T8 |
| R9 | T9 |
| R10 | T10 |
| R11 | T11 |
| R12 | T12 |
| R13 | T13 |
| R14 | T14 |
| R15 | T15 |
| R16 | T16 |
| R17 | T17 |
| R18 | T18 |
| R19 | T19 |
| R20 | T20 |
| R21 | T21 |
| R22 | T22 |
| R23 | T23 |
| R24 | T24 |
COVERAGE: R1 -> T1
COVERAGE: R2 -> T2
COVERAGE: R3 -> T3
COVERAGE: R4 -> T4
COVERAGE: R5 -> T5
COVERAGE: R6 -> T6
COVERAGE: R7 -> T7
COVERAGE: R8 -> T8
COVERAGE: R9 -> T9
COVERAGE: R10 -> T10
COVERAGE: R11 -> T11
COVERAGE: R12 -> T12
COVERAGE: R13 -> T13
COVERAGE: R14 -> T14
COVERAGE: R15 -> T15
COVERAGE: R16 -> T16
COVERAGE: R17 -> T17
COVERAGE: R18 -> T18
COVERAGE: R19 -> T19
COVERAGE: R20 -> T20
COVERAGE: R21 -> T21
COVERAGE: R22 -> T22
COVERAGE: R23 -> T23
COVERAGE: R24 -> T24
TASKS_RESULT: PASS (24/24 requirements covered by 24 task(s))
